# Hardcoder
# 🎓 AI Student Churn Prediction API

**AI-модуль прогнозирования оттока студентов для Softclub CRM**

## 📋 Описание

Backend API для прогнозирования риска отчисления студентов с использованием Machine Learning и Large Language Models.

### Основные возможности

- ✅ **Прогнозирование риска** - ML модель классифицирует студентов (Low/Medium/High risk)
- ✅ **AI-объяснения** - LLM генерирует понятные объяснения причин риска на русском языке
- ✅ **Рекомендации** - Система предлагает оптимальные действия для удержания студента
- ✅ **REST API** - FastAPI с автодокументацией

## 🏗️ Архитектура

```
app/
├── models/          # ML модель и LLM сервис
├── api/             # FastAPI роуты и схемы
├── core/            # Конфигурация
├── data/            # Моковые данные
└── utils/           # Утилиты

models/trained/      # Обученные ML модели
notebooks/           # Jupyter ноутбуки для обучения
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Используя uv (рекомендуется)
uv sync

# Или активируй виртуальное окружение
source .venv/bin/activate
```

### 2. Настройка переменных окружения

```bash
# .env файл уже создан с GROQ_API_KEY
# Проверь что ключ на месте
cat .env
```

### 3. Запуск сервера

```bash
# Режим разработки
python main.py

# Или через uvicorn напрямую
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 4. Тестирование API

Открой в браузере:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 📡 API Endpoints

### GET `/api/students/risks`
Получить список всех студентов с оценкой риска

**Response:**
```json
{
  "total": 50,
  "students": [
    {
      "student_id": 1,
      "student_name": "Студент 1",
      "risk_level": "High",
      "confidence": 0.85
    }
  ]
}
```

### GET `/api/students/{student_id}/analysis`
Детальный анализ студента с AI-объяснениями

**Response:**
```json
{
  "student": {...},
  "risk_level": "High",
  "confidence": 0.85,
  "explanation": "Риск высокий, потому что студент пропустил 4 занятия подряд...",
  "recommendation": {
    "action": "Звонок от ментора",
    "reason": "Личный контакт поможет выяснить причину...",
    "success_probability": 0.75,
    "urgency": "high"
  },
  "key_factors": {
    "attendance_rate": 0.25,
    "missed_classes_streak": 0.20
  }
}
```

### GET `/api/health`
Проверка работоспособности системы

## 🤖 Технологии

- **FastAPI** - современный async веб-фреймворк
- **XGBoost** - ML модель для классификации
- **Groq** - быстрый LLM provider (Llama 3.3 70B)
- **Pydantic** - валидация данных
- **Python 3.12+**

## 📊 Фичи для ML модели

- `attendance_rate` - процент посещаемости
- `homework_completion` - процент выполненных ДЗ
- `payment_delays` - количество задержек оплаты
- `days_since_last_payment` - дней с последней оплаты
- `test_avg_score` - средний балл тестов
- `communication_activity` - активность общения
- `days_enrolled` - дней в системе
- `missed_classes_streak` - пропусков подряд

## 🔧 Разработка

### Обучение ML модели

```bash
# TODO: Создать notebook для обучения
jupyter notebook notebooks/train_model.ipynb
```

### Тестирование

```bash
# TODO: Добавить тесты
pytest tests/
```

## 📝 TODO

- [ ] Обучить XGBoost модель на реальных данных
- [ ] Добавить SHAP values для explainability
- [ ] Создать frontend dashboard
- [ ] Добавить unit tests
- [ ] Добавить логирование
- [ ] Интеграция с реальной CRM

## 👨‍💻 Автор

Softclub CRM Team
