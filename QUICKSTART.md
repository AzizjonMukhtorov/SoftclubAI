# 🚀 Быстрый Старт - AI Student Churn Prediction

## ✅ Что Уже Сделано

- ✅ ML модель обучена (55.5% accuracy)
- ✅ LLM интегрирован (Groq API)
- ✅ FastAPI backend готов
- ✅ Все зависимости установлены
- ✅ Все протестировано успешно

---

## 🏃 Запуск Сервера (3 шага)

### 1. Открой терминал
```bash
cd /Users/nodir/Desktop/srm-softclub
```

### 2. Активируй виртуальное окружение
```bash
source .venv/bin/activate
```

### 3. Запусти сервер
```bash
python main.py
```

Сервер запустится на: **http://localhost:8000**

---

## 📡 Тестирование API

### Вариант 1: Swagger UI (Рекомендуется для демо)

Открой в браузере: **http://localhost:8000/docs**

Там можно интерактивно тестировать endpoints!

### Вариант 2: Командная строка

```bash
# Список всех студентов с риском
curl http://localhost:8000/api/students/risks

# Детальный анализ студента #5
curl http://localhost:8000/api/students/5/analysis
```

---

## 🧪 Тест перед демо

```bash
# Запусти тестовый скрипт
source .venv/bin/activate
python test_api.py
```

Должно вывести:
- ✅ ML predictions работают
- ✅ LLM генерирует объяснения
- ✅ Все 5 студентов проверены

---

## 🎯 Для Демо Жюри

1. Запусти: `python main.py`
2. Открой: http://localhost:8000/docs
3. Покажи endpoint: `/api/students/risks` → список студентов
4. Покажи endpoint: `/api/students/5/analysis` → детальный анализ
5. Объясни: ML модель → LLM объяснение → REST API

---

## 📁 Важные Файлы

- `main.py` - Запуск сервера
- `test_api.py` - Тестирование
- `train_model.py` - Обучение модели (уже обучена)
- `models/trained/churn_model.json` - Обученная ML модель
- `.env` - Groq API ключ

---

## ❓ Если Что-то Не Работает

### Проблема: Модуль не найден

```bash
source .venv/bin/activate
pip install fastapi uvicorn pydantic groq xgboost scikit-learn
```

### Проблема: Groq API не работает

Проверь `.env` файл - там должен быть `GROQ_API_KEY`

### Проблема: Порт 8000 занят

Измени в `main.py`:
```python
uvicorn.run("main:app", port=8001)  # используй другой порт
```

---

## ✨ Готово!

Система работает! Удачи на хакатоне! 🚀

**Документация:**
- Полная архитектура: `implementation_plan.md`  
- Результаты тестов: `walkthrough.md`
- Этот файл: `QUICKSTART.md`
